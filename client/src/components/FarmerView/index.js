import React from 'react';
import AuthTimer from '../AuthTimer';
import FarmerLayout from '../FarmerLayout';

const FarmerView = props => (<AuthTimer><FarmerLayout {...props} /></AuthTimer>);

export default FarmerView;
